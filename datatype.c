/*
repeat n-1 times 
    for i from  0 to n-2
        if numbers[i] and numbers [i+1] out of order
            swap them
    if no swaps 
        quit

        
comparison sorting algorithm runtime complexity 

    O(n^2)- bubble sort(w)
    O(n log n) - merge sort(w,b) 
    O(n) - bubble sort(b) 
    O(log n)
    O(1)

    -----merge sort-----
    if only one number 
        quit
    else
        sort left half od numbers
        sort right half of numbers
        merge sorted halves

*/