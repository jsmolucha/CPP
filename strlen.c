#include<stdio.h>

int main(void) {
    char string[100], i;
    printf("enter a string: ");
    scanf("%string", string );

    for (i = 0; i < string[i] != '\0'; i++) {

    }
    printf("The string has a character count of: %d", i);
    return 0;
}